@include('partials/header')
@include('partials/flash')

<h3>Flashcards</h3>

<div class="deck-grid">
    @each(deck in decks)
      <div class="deck-card">
        <div>
          <h4>{{ deck.title }}</h4>
          <p>{{ deck.description }}</p>
        </div>
        <a href="/decks/{{ deck.id }}" class="cta-button">Voir le deck</a>
      </div>
    @end
</div>

@include('partials/footer')
